using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// NOTE: The "text" box height needs to expand when more text is added to make the scrollbar grow.
//       Also need to scroll the text down when new text is added: //Ex: scrollRect.velocity = new Vector2 (0f, 1000f);

public class ChatBox : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
